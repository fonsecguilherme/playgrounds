import 'package:flutter/material.dart';

class SvgMichael extends StatelessWidget {
  const SvgMichael({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Michael'));
  }
}
