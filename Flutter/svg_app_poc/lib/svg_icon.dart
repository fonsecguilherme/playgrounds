import 'package:flutter/material.dart';

class SvgIcon extends StatelessWidget {
  const SvgIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('SvgIcon'));
  }
}
