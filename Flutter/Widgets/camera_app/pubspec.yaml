name: camera_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  # camera: ^0.11.0
  image_picker: ^0.8.7+3
  flutter:
    sdk: flutter
  video_player: ^2.9.2
  # path: ^1.9.0
  # path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
